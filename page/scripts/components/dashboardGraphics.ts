export const dashboardGraphics = (container: HTMLDivElement) => {
   container.insertAdjacentHTML("beforeend", `
            <section class="dashboard-graphics line-spacing">
               <span>🌐 Usuário online o deashboard será carregado... 📊</span>
            </section>
      `);
}